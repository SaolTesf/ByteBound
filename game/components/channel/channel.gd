class_name Channel extends RefCounted
## Colour channel linking a button (pedestal / pressure plate) to the [Laser] or
## door it toggles. A button and the thing it controls share the same channel.
##
## A type holder for the [enum Type] enum — never instantiated.

enum Type { RED, GREEN, BLUE }
